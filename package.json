{
  "name": "spot-the-difference",
  "version": "1.0.0",
  "description": "A spot the difference game built with HTML5, CSS, and modular JavaScript",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-assets": "node scripts/generate-assets.js"
  },
  "devDependencies": {
    "@vitest/ui": "^1.0.0",
    "happy-dom": "^12.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}